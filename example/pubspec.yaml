name: audio_io_example
description: Demonstrates how to use the audio_io plugin.
publish_to: "none"

environment:
  sdk: ">=2.417.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.3.1

  audio_io: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
